{
  "name": "lightweight-debounce-throttle",
  "version": "0.0.1",
  "description": "Lightweight TypeScript native debounce and throttle",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && esbuild src/index.ts --outfile=dist/index.js && tsc --emitDeclarationOnly",
    "test": "vitest --run"
  },
  "author": "minojiro <minojiro@users.noreply.github.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.38",
    "typescript": "^4.6.4",
    "vitest": "^0.10.0",
    "c8": "^7.11.2"
  }
}
